name: Build and test StringCalculator

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore StringCalculator.csproj

      - name: Build
        run: dotnet build StringCalculator.csproj --configuration Release --no-restore

      - name: Test
        run: dotnet test StringCalculator.csproj --configuration Release --no-build --verbosity normal
